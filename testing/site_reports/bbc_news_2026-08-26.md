# SITE TEST REPORT — BBC News (CONTAMINATION-EVIDENCE — DO-NOT-CITE)

## 1. Metadata

| Field | Value |
|---|---|
| Site | BBC News |
| URL | https://www.bbc.com/news |
| Test date | 2026-08-26 |
| Run ID | `run_20260826_000112` |
| Status | **RETRACTED — CONTAMINATED / DO-NOT-CITE** |

## 2. Why this run is retracted

The run folder carries a `CONTAMINATION_MARKER` (folder_purity: pure=false):

```
Catalog host histogram: bbc ×10 + magento ×1 + eviltester ×1
CONTAMINATION_REJECTS.json: 3 foreign exploration-results rejected at
collect time by the provenance guard
serial-B purity-FAIL disposition at ~08:3x IST: "NO INDEX row, NO report patch"
```

Despite that disposition, the row was registered via a one-off repair script
(testing/fix_rows_27_37.js) without honoring the purity failure, and with a
report that omitted it. The independent gate audit (T401, finding F7-01,
CRITICAL) caught the registration. This report now serves ONLY as
contamination evidence.

## 3. What is valid in the folder

- Arithmetic integrity: FT 5/7, fusion-attributable 77.8% (= 5/7 of 9 final
  tests) recomputes exactly from raw artifacts
- The provenance guard itself fired correctly at collection time — the
  failure was chaining a pre-fix folder into S4/FT/dashboard afterward,
  not a guard miss

## 4. Remediation path

A clean sequential bbc re-run per serial-B's posted recipe would restore the
row. Deferred as DO-NOT-CITE pending quota windows; the campaign dataset
stands complete WITHOUT this row.

## 5. Lesson recorded

Registration scripts must run testing/folder_purity.js as a hard precondition
and refuse to write INDEX rows for impure folders. Logged alongside defects
#20–#24.
