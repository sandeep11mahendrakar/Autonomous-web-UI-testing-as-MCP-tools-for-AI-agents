# Guru99 Bank Demo

## 1. Metadata

| Field | Value |
|---|---|
| Site | `Guru99 Bank (demo V4)` |
| URL | `https://demo.guru99.com/V4/` |
| Test date | 2026-08-26 |
| Unified run ID | `run_20260826_020711` |
| Run folder | `runs/run_20260826_020711/` |
| LLM provider / model (A, B, Fusion) | openrouter / stealth/ox-alpha (reasoning=low) |
| Repo state | branch after-tier-2 @ post-D9 (final batch W1 window) |
| Explorer | W1 / ox-alpha serial-1 (final-batch site #36) |
| Report status | FINAL |

## 2. Verdict snapshot

| Stage | Status | Headline number | Artifact source |
|---|---|---|---|
| A exploration | ✅ SUCCESS | 25 steps / **20 states** / 12 URLs, 3 clicks + 8 fills, 0 errors (`max_states_reached`) | `runs/<id>/dom/exploration_summary.json` |
| A test generation | ✅ | **3 grounded tests** (first A-side generation in my Tier-3 runs — D7 budget validated) | `runs/<id>/dom/test_cases.json` |
| B exploration | ✅ success | `no_candidates_remaining`, execution stage complete | `runs/<id>/vision/outputs/*_exploration_result.json` |
| B test execution | ✅ | **1/1 PASS**, STRONG verification, 0 weak / 0 stale / 0 unresolved | `runs/<id>/vision/outputs/execution_results.json` |
| S1 catalog | ✅ | 54 elements / 27 behaviors / 13 pages / only 2 conflicts | `runs/<id>/fusion/catalog.json` |
| S2 gap report | ✅ ran | fed S4 pool | `runs/<id>/fusion/gap_report.json` |
| S4 fusion synthesis | ✅ | 18 offered → **8 accepted** / 4 rejected, grounded | `runs/<id>/fusion/fusion_report.json` |
| FT live execution | ✅ | **4/8 PASS** (steps 15/19), 4 targets preverified, 0 warnings | `runs/<id>/fusion/ft_execution_results.json` |
| S6 dashboard | ✅ | **66.7% fusion-attributable** (12 final = A3/B1/fusion8) | `runs/<id>/fusion/dashboard_data.json` |

One-paragraph verdict: cleanest full-pipeline run of the Tier-3 campaign.
folder_purity PURE 4/4; manifest SUCCESS on BOTH architectures. This is the
first site where the D7 `ARCH_A_TIMEOUT_MS=1500000` budget demonstrably
changed the outcome: A used 1069s (> old 900s cap), hit `max_states_reached`
as designed instead of a timeout cliff, and generated 3 grounded tests — so
the final suite is a genuine A(3)+B(1)+fusion(8) mix rather than an all-fusion
salvage. Login-form surface mapped read-only per policy; no credential entry.

## 3. Architecture results

### 3.1 Architecture A (DOM)
- 25 steps across 12 URLs of the demo bank (login page + manager pages
  reachable without auth via direct nav); 8 fills exercised form inputs,
  3 clicks navigations.
- Terminated `max_states_reached` (20-state trim limit) — budget consumed by
  design, not by failure.
- Generated TC001–TC003 grounded login/form-error workflows.

### 3.2 Architecture B (vision)
- Clean pass: single replay test PASSED with input-level (STRONG)
  verification; no OCR trouble on the sparse classic-HTML layout.

### 3.3 A/B comparison notes
- Only 2 catalog conflicts (this site's static markup is unambiguous).
- Complementary coverage: A contributed breadth (manager pages), B verified
  the login flow end-to-end; S4 bridged form-error paths neither walked alone.

## 4. SITE bugs detected
None pursued — read-only policy. Empty-credential submit behavior was
exercised by explorers as form-validation surface (recorded as expected
demo behavior, not a bug).

## 5. PIPELINE bugs & fixes found during this test
none — clean run. (Stale lock from a dead PID cleared before launch per
PID-liveness rule; driver held lock for the whole cycle.)

## 6. Where the project lagged
- FT 4/8: the 4 fails concentrate on deep manager-page flows whose steps
  depend on session state the read-only policy intentionally does not create
  (no logins). Honest ceiling, not a defect.
- B-side url count again reads 0 in extraction despite success (known
  cosmetic extractor gap, already flagged on #21 wikipedia).

## 7. Metrics table

```
A: steps=25 states=20(urls=12) clicks=3 fills=8 errors=0 (max_states_reached, 1069s SUCCESS)
B: steps=n/a(no_candidates_remaining) generated_tests=1 replay=1pass/0fail STRONG
S1: elements=54 behaviors=27 pages=13 conflicts=2
S4: offered=18 candidates=14 accepted=8 rejected=4 grounded=true
FT: total=8 passed=4 failed=4 steps 15/19 preverified=4 warnings=0
Dashboard: total_tests=12 (A=3 B=1 fusion=8) ; pct_fusion=66.7%
Offline suites after run: see commit message (green at commit time)
Duration: A=1069s B=308s S4 call=41s FT exec=111s
```

## 8. Asset index

| Asset | Path |
|---|---|
| Run manifest | `runs/run_20260826_020711/run_manifest.json` |
| A memory log / test cases | `runs/run_20260826_020711/dom/memory_log.json`, `dom/test_cases.json` |
| A screenshots | `runs/run_20260826_020711/dom/screenshots/` |
| B outputs | `runs/run_20260826_020711/vision/outputs/` |
| Fusion artifacts | `runs/run_20260826_020711/fusion/` |
| Extract snapshot | `testing/extract_run_20260826_020711.json` |

## 9. Recommendations for next runs
1. Adopt `ARCH_A_TIMEOUT_MS=1500000` as default for ALL future tiers — first
   run where the budget change flipped the architecture mix (A tests present).
2. If auth flows are ever in scope, seed creds explicitly like CURA re-run;
   otherwise manager-page FT passes are capped by policy by design.
3. Good regression smoke candidate alongside hackernews (small DOM, fast).

## 10. Reproduction commands

```bash
node testing/tier3_w1.cjs guru99_bank https://demo.guru99.com/V4/
node testing/folder_purity.js run_20260826_020711   # pure=true required
node testing/extract_run.js run_20260826_020711
```
