# SYSTEM IMPROVEMENTS — consolidated agent suggestions

Status legend: IMPLEMENTED (landed, evidence path) / PARKED (human-deferred) /
V2-QUEUED (ranked post-campaign). Sources: MASTER (this section), serial-1..N
workers, WORKER-2, AUDITOR, SUB-MASTER. Consolidated by serial-B (T614,
2026-08-27): every row from every source is preserved below — nothing dropped
— then merged into one ranked table (§4) cross-referenced to
`docs/V2_ROADMAP.md` (R1–R12).

## MASTER suggestions (from running the full campaign as coordinator)

| # | Suggestion | Category | Status | Notes |
|---|---|---|---|---|
| M1 | Single-source ledger regeneration (regen_ledger pattern) for every derived doc | process | IMPLEMENTED (testing/regen_ledger.js) | INDEX/EVALUATION/VTQ can never disagree |
| M2 | Provenance filter in artifact collection (host match vs manifest) | arch | IMPLEMENTED (runBoth.js) | rejects logged to CONTAMINATION_REJECTS.json; extend to ALL output file types (open gap: test_cases/execution_results still sweep — see magento/eviltester skips) |
| M3 | KNOWN_ALIASES redirect table with citations | perception | IMPLEMENTED (runBoth.js + folder_purity.js) | testmuai rebrand case |
| M4 | PID-liveness lockfile checks | process | IMPLEMENTED (drivers) | dead-PID false aborts eliminated |
| M5 | Quota-aware scheduler as first-class component | process | PARTIAL (ad hoc in night_chain/rerun drivers) | promote to shared lib: detect TPD/TPM patterns, budget ledger per run |
| M6 | Session-scoped vision storage (structural stitching fix) | arch | V2-QUEUED | kills the mtime-window class entirely |
| M7 | Dynamic port allocation for vision services | arch | V2-QUEUED | enables safe parallel pipelines |
| M8 | Shared mutex across ALL study drivers | process | PARTIAL (.campaign.lock exists; single-flight not universally honored) | enforce via lock helper lib |
| M9 | Fail loudly on parse_failed (never convert to done) | executor | IMPLEMENTED (llmClient honesty path verified by auditor) | keep as invariant test |
| M10 | Minimum verification-strength floor for PASS | executor | V2-QUEUED | weak-signal passes inflate quality metrics |
| M11 | Value-oracle synthesis | research | V2-QUEUED (top item) | spec pending T615 recreation |
| M12 | Clock protocol: UTC+IST dual stamps, master broadcasts canonical time in directives | process | PARTIAL (directives carry IST; drift incidents recurred) | consider relative timestamps (+12m from claim) |
| M13 | Per-agent append-only comms files replacing shared board region | intercom | IMPLEMENTED pattern (docs/comms/) | board rows stay for state only |
| M14 | Integrator-only main branch (workers push agent/<name>) | intercom | ADOPTED AD HOC by workers | make it the documented rule |
| M15 | Global STOP-file kill switch all drivers honor | process | NOT BUILT | sleep orders were manual posts |
| M16 | Repo description/topics + release assets on GitHub | ship | OPEN | web UI action, user-side |

## WORKER suggestions (serial-1/1A, W3, W4, W5, FINAL-BATCH, replacement lane)

| # | Source | Suggestion | Category | Status → roadmap |
|---|---|---|---|---|
| W1 | RETROSPECTIVE_TIER3 §5.1 | Watchers must re-check board claims, not just lock files (`--claim <id>` driver arg makes claims mechanical) | process | V2-QUEUED → R10/R12 |
| W2 | RETROSPECTIVE_TIER3 §5.2 | Gate census wording generated by script from INDEX rows, never typed ("15/15" overstatement class, audit F5-01) | integrity | V2-QUEUED → R12 |
| W3 | RETROSPECTIVE_TIER3 §5.3 | Blank-render probe as standard preflight step (5s headless screenshot check; goodreads/archive.org class) | preflight | V2-QUEUED → R8-adjacent |
| W4 | RETROSPECTIVE_TIER3 §5.4 | External-link policy decision: record-and-stay beats block (foreign links become S2 coverage signals instead of purity failures); ties to DEFECT #25 | policy/arch | V2-QUEUED → R2 bundle |
| W5 | RETROSPECTIVE_TIER3 §5.5 | Per-window run-dir prefixes (`run_<worker>_<ts>`) make stitching visually obvious pre-guard | process | V2-QUEUED → R12 |
| W6 | RETROSPECTIVE_TIER3 §6.1 | Close F5-01: chain or reclassify #27 bbc_news (guard 97a29cb lineage makes chaining safe now) | integrity | DONE post-audit (row 27 retracted DO-NOT-CITE per F7-01) |
| W7 | RETROSPECTIVE_TIER3 §6.2 | DEFECT #25 href-guard fix + regression test (luma.com repro `run_20260826_003258`) | executor | V2-QUEUED → R2 |
| W8 | RETROSPECTIVE_TIER3 §6.3 | S4 parameterized-href resolution (`/item` → `?id=N`) + hackernews cheap re-run | grounding | V2-QUEUED → R7 |
| W9 | RETROSPECTIVE_TIER3 §6.5 | SPA hydration wait strategy (unlocks archive.org/goodreads/Juice-Shop class) | perception | V2-QUEUED → R8 |
| W10 | FINAL-BATCH C4.1 | #40 w3schools re-run = cheap win once single-launch discipline holds | campaign ops | V2-QUEUED → R12 |
| W11 | FINAL-BATCH C4.3 | B screenshot-fatal retry (two occurrences: gutenberg, dynamic_loading) — promote to minor-fix shortlist | executor | V2-QUEUED → R12 |
| W12 | serial-B D9 window | Extend collectArchitectureB provenance check to EVERY session-id artifact (test_cases_*/execution_results/exploration_history) — 4 repro cases attached; fail-closed not fail-open (F4-05) | arch | PARTIAL (97a29cb covered exploration_result+test_cases+execution_results) → R3 completion |
| W13 | serial-B T609 | npm pack ships `.env.example` placeholders only — keep an invariant test asserting `.env` never enters tarballs | ship hygiene | V2-QUEUED → R12 |

## AUDITOR suggestions (AUDITOR-3 + post-freeze audits)

| # | Suggestion | Category | Status → roadmap |
|---|---|---|---|
| A1 | Audit E risk 1/3: concurrency architecture (newest-dir picking + shared storage + fixed ports) is the top structural risk | arch | V2-QUEUED → R1 |
| A2 | Audit E risk 2: parse_failed silent-stop | executor | IMPLEMENTED (M9) |
| A3 | Audit E risk 4: PASS-by-default verification ladder bottoms at body-text>100 chars | executor | V2-QUEUED → R4/M10 |
| A4 | Audit E risk 5: shell-string orchestration in drivers (injection surface) | arch | V2-QUEUED → R12 |
| A5 | CI-style invariant before ANY report generation: manifest.url == requested URL AND catalog hosts ⊆ target∪visited | integrity | IMPLEMENTED (folder_purity.js + run_attribution.js, suite-covered) |
| A6 | One mutex shared by ALL drivers + dynamic ports | arch | V2-QUEUED → R1 (=M7/M8) |
| A7 | Require ≥MEDIUM verification for PASS; start assertion-oracle work | executor/research | V2-QUEUED → R4 (=M10/M11) |

## SUB-MASTER additions

| # | Suggestion | Category | Status → roadmap |
|---|---|---|---|
| S1 | Post-execution QA of worker manifest runs (caught real drift twice) | integrity | KEEP as standing practice |
| S2 | Tracked per-agent comms mirrors while board is untracked | intercom | IMPLEMENTED (docs/comms/) (=M13) |

## §4 Consolidated ranked table (every row above maps to exactly one entry)

| Rank | Item | Absorbs | Roadmap ref |
|---|---|---|---|
| 1 | PARALLEL_SPEC D1–D3 bundle: dynamic ports + runBoth singleton lock + session-scoped storage | M6, M7, M8, A1, A6 | R1 |
| 2 | Value/assertion-oracle synthesis (+ min-verification floor) | M10, M11, A3, A7 | R4 |
| 3 | Provenance-guard scope completion (all session-id artifacts, fail-closed) | M2-gap, W12 | R3 |
| 4 | DEFECT #25 href-goto guard + external-link record-and-stay policy | W4, W7 | R2 |
| 5 | S4 parameterized-href resolution + hackernews re-run | W8 | R7 |
| 6 | SPA hydration wait strategy | W9 | R8 |
| 7 | Capability flags S1 + executability filter v2 (T604) | (parked batch) | R5 |
| 8 | Acceptance-rate tightening prompt+validator (T605) | (parked batch) | R6 |
| 9 | A/B identity reconciler | (retro Tier-2 batch) | R9 |
| 10 | Post-chain idempotency + claim-aware queue runner | W1 | R10 |
| 11 | Cross-platform port (Windows ⇄ POSIX) | A4-adjacent | R11 |
| 12 | Polish batch: script-generated census wording, run-dir prefixes, blank-render preflight, w3schools re-run, screenshot-fatal retry, pack-invariant test, quota scheduler lib, STOP-file, clock protocol, integrator-only branches, repo topics/release assets | W2, W3, W5, W10, W11, W13, M5, M12, M14, M16, S1 | R12 |

Already-IMPLEMENTED rows preserved above (M1, M3, M4, M9, M13, A2, A5, S2)
are closed and carry their evidence paths in the MASTER/worker tables.

## Where to implement

`docs/V2_ROADMAP.md` (restored from e11f646, R1–R12) is the execution-order
document; this file is the suggestion ledger proving nothing was lost between
sources. Discrepancies resolve in favor of the roadmap ranking.
